import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        justifyContent: 'center',
        marginLeft: '10%',
        marginRight: '10%',
        marginTop: '50%'
    }
});

export { styles };